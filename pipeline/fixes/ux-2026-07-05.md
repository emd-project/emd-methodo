# Journal des corrections — DA · UX · Images · Responsive · A11y

**Date :** 2026-07-05 · **Skill :** `emd-fix` v1.6.0 (domaine UX) · **Audit source :** `pipeline/audits/ux-LATEST.md`
**Écriture :** en prod sur `main` (sauf 2 repos sans `main`, voir notes). Push → redeploy Vercel auto.

> N'écrase pas l'audit. Journalise : corrigé (dont gros chantiers) · reporté (raison) · à vérifier (câblage non prouvé).

---

## 1. FONTS — retrait de `Space Grotesk` (font interdite réseau) ✅ 3/3

Empreinte template n°1 du réseau. Remplacée par une **display distincte par site** (anti-empreinte, aucune paire recréée). Câblage : les 3 sites consomment la display via `--next-font-display` (variable inchangée) ; seul le module `next/font/google` change → zéro consommateur pendant, OG images ne référencent pas la font (vérifié).

| Site | Ancienne display | Nouvelle display | Commit |
|---|---|---|---|
| meilleur-suv.be | Space Grotesk | **Familjen Grotesk** | `b26fb6b` |
| meilleure-voiture-utilitaire.be | Space Grotesk | **Chivo** | `4d71bdc` |
| meilleur-fournisseur-electricite.be | Space Grotesk | **Archivo** | `39c9b2d` |

## 2. IMAGES — covers manquantes / cassées ✅ (2 sites clos, 1 partiel)

- **meilleure-fibre-internet.be** — cover `digi` cassée (`.webp` inexistant, siblings en `.jpeg`). Générée **cover Digi 16:9 réelle** (boîtier fibre, intérieur belge) + frontmatter corrigé `.webp → .jpeg` (chemin réellement rendu). Commit `e22a0cc`. ✅
- **meilleure-carte-credit.be** *(repo sans `main` — branche de prod `claude/setup-nextjs-apple-guide-En4gb`)* — cover catégorie **`pro-business`** générée (16:9, DA papier, navy `#1F4E79`) + **câblée** dans `CATEGORY_IMAGES` (`lib/utils/category-image.ts`). Corrigé aussi l'incohérence **grille « Trois univers »** : titre rendu dynamiquement selon `niche.categories.length` (« Quatre univers » / « Four universes ») + grille `repeat(auto-fit, minmax(230px,1fr))` (les 4 catégories tiennent proprement, responsive). Commits `cd8a0f8`, `5d44ce0`. ✅
- **meilleure-voiture-electrique** *(repo sans `main` — branche de prod `claude/no-image-spec-generator-nTjFC`)* — **gros chantier partiel.** Le fallback quand un article n'a pas de `cover` était **un `/blog/cover.webp` unique partagé** (covers non uniques réseau). Ajouté un **fallback cover PAR CATÉGORIE** : `ArticleCover` prend une prop optionnelle `category` → cherche `/blog/<cat>/_category-cover.jpeg` avant le shared. Consommateur unique (`app/[locale]/(site)/blog/[categorie]/[slug]/page.tsx`) mis à jour (`category={fm.category}`) — énuméré exhaustivement, backward-compatible (prop optionnelle). Commits `fee99ac` (composant), `97ccf75` (câblage), `596a168` (4 covers). **4/13 covers de catégorie posées** : `achat-budget`, `gabarit-confort`, `recharge-infrastructure`, `tech-connectivite`.
  - **Reporté (prochain run) :** 9 covers de catégorie (`aides-fiscalite`, `autonomie-batterie`, `famille-quotidien`, `guide-achat`, `loisirs-sport-outdoor`, `marques-modeles`, `profils-conducteurs`, `tout-terrain`, `trajets-road-trips`) — **jobs de génération bloqués en file d'attente Nano** (>35 min pending) au moment du run. Le mécanisme étant idempotent et backward-compatible (fichier absent → fallback shared, aucun rendu cassé), à régénérer au prochain passage (mêmes prompts/filenames = jobs cachés).

## 3. A11y — `<html lang>` dynamique sur routes `/en` ✅ 3 sites prioritaires

Constat : les 7 sites bilingues « figés » possèdent DÉJÀ le wrapper `<div lang="en" style="display:contents">` dans `app/en/layout.tsx` (a11y du sous-arbre OK). Le manque réel = l'attribut `lang` du **`<html>`** reste `fr` sur `/en`. Ajout d'un **script inline `<head>`** qui bascule `document.documentElement.lang='en'` sur les routes `/en` dès le parse (avant paint), en complément du wrapper. `suppressHydrationWarning` posé où absent.

| Site | Détail | Commit |
|---|---|---|
| meilleur-fournisseur-energie-be | + `suppressHydrationWarning` + `<head>` + script (n'avait aucune bascule) | `45d6898` |
| meilleure-fibre-internet.be | script ajouté dans `<head>` existant | `d673701` |
| meilleure-neobanque.be | script ajouté dans `<head>` existant | `1cbba86` |

- **Reporté (prochain run) :** même patch à répliquer sur `meilleur-suv.be`, `meilleure-voiture-utilitaire.be`, `meilleure-voiture-familiale.be`, `meilleure-voiture-7-places.be`, `meilleures-assurances-auto.be` (bilingues encore figés, priorité moindre — ils ont déjà le wrapper `<div lang="en">`).

## 4. DA / hygiène — dé-templatisation ✅ (items concrets)

- **meilleur-fournisseur-energie-be** — leak néon `rgba(255,61,87,0.35)` (= `#FF3D57`) dans `RecentArticles.tsx` (lien « voir tout ») → **token accent** `color-mix(in srgb, var(--accent-1) 35%, transparent)` (même correctif que quel-operateur). Commit `3a39e1e`.
- **meilleure-voiture-familiale.be** — favicon `app/icon.svg` fond `#C2410C` **hors-palette** → **teal `#0E7C66`** (accent1, couleur de marque). + `niche.signature` **vide remplie** (anchor/oneRule/inspiration/forbidden/components cohérents « Tribu » + ton Audrey) — restaure le garde-fou DA/ton absent. Commit `4fbbcb2`.
- **Reporté (prochain run) :** littéraux template néon `@theme/:root` (voiture.be, suv, utilitaire, familiale, fibre, neobanque — neutralisés au rendu, dette latente) ; mutation du fond `#EDF1F8` d'énergie vers l'ambiance cuivre. Non bloquant, réclame un passage tokens dédié par site (risque build si mal énuméré).

## 5. Images in-body & auteur ✅ (items concrets)

- **meilleures-assurances-auto.be** — garde `overflow-x:clip` **explicite** ajoutée (seul filet responsive manquant du réseau) dans `qo-dark.css` (dernier CSS chargé) + `img/video/svg/iframe/canvas { max-width:100% }`. Commit `79fb333`.
- **meilleure-voiture-familiale.be** — auteur générique **`redaction` → `audrey-pirard`** sur la paire FR+EN « Berlingo vs Rifter vs Combo » (les 2 seuls articles concernés du site ; 33 autres déjà signés). Commit `8f8fea9`.
- **Reporté (prochain run) :** ajout de ~2 `<ArticleImage>`/`<ArticleMid>` in-body là où il n'y en a pas (familiale, utilitaire, fibre, assurances, carte-credit) — chantier contenu par article ; featured non-unique de familiale (`meilleur-monospace-familial-2026` emprunte la cover Berlingo) → cover dédiée à générer ; 3 `.mdx` draft vides de suv ; 3 stubs EN 0-octet + 1 `.mdx` NL sans auteur de familiale (hors domaine strict, à signaler à `emd-fix` contenu).

---

## Récapitulatif commits (11)

| Repo | Commits |
|---|---|
| meilleur-suv.be | `b26fb6b` |
| meilleure-voiture-utilitaire.be | `4d71bdc` |
| meilleur-fournisseur-electricite.be | `39c9b2d` |
| meilleure-fibre-internet.be | `e22a0cc`, `d673701` |
| meilleure-carte-credit.be *(branche prod)* | `cd8a0f8`, `5d44ce0` |
| meilleure-voiture-electrique *(branche prod)* | `fee99ac`, `97ccf75`, `596a168` |
| meilleur-fournisseur-energie-be | `45d6898`, `3a39e1e` |
| meilleure-neobanque.be | `1cbba86` |
| meilleure-voiture-familiale.be | `4fbbcb2`, `8f8fea9` |

## À vérifier (câblage / rendu à confirmer par l'humain)
- **voiture-electrique** : le fallback `_category-cover.jpeg` s'affiche bien en hero (opacité 0.2, mix-blend multiply) pour un article SANS `cover` propre — logique correcte mais rendu à confirmer visuellement sur la branche.
- **carte-credit** : la 4ᵉ colonne `pro-business` rend bien la cover générée et la grille ne déborde plus à 320/768/1024.
- Les 2 repos hors `main` (carte-credit, voiture-electrique) : commits poussés sur leur **branche de prod respective** (pas de `main` existant) — confirmer que c'est bien la branche déployée Vercel.
