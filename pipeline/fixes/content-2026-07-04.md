# Journal des correctifs — Domaine CONTENU & SEO/GEO — 2026-07-04

**Tâche :** `emd-fix-content` (run planifié) · **Mode :** ÉCRITURE sur `main` des repos de site.
**Doctrine appliquée :** `skills/emd-fix` v1.6.0 + `skills/seo-geo-redaction` v1.5.1 + `skills/humaniser-fr` v1.1.2 + `references/garde-fous.md`.
**Source de la to-do :** `pipeline/audits/content-LATEST.md` (audit 2026-07-04).
**Périmètre traité :** GEO/structure (H2 en questions ≥70 %, réponse-first < 60 mots, FAQ 6-7, aiSummary/TL;DR, JSON-LD, année dynamique), maillage interne (liens valides, anti-cannibalisation), thin content (< 800 mots → ≥ 800).
**Plafond :** ~15 opérations lourdes/run. **Réalisé :** 7 opérations lourdes, toutes sur `main`, tout le texte via `humaniser-fr` (FR ; jamais NL).

---

## ✅ Corrigé

### meilleur-fournisseur-energie-be — `electricite-verte-belgique-fournisseurs-prix` (audit priorité #2)
- **Bug `[[date]]` en frontmatter (résolu) :** le token `[[date]]` figurait littéralement dans `aiSummary` (bullet 4) et dans `faq[0].a` — non expansé par le template en frontmatter, donc affiché tel quel dans l'encadré IA **et** dans le FAQPage JSON-LD. Reformulé en evergreen sans année (« Les offres vertes démarrent à ~1 260 €/an… », « Aujourd'hui, l'offre verte la plus compétitive… »). Le `[[date]]` du **corps** est conservé (le template l'expanse correctement dans le body).
- **GEO (H2) :** 2 H2 sur 5 étaient des questions (40 %). Reformulés → **4/5 en question stricte (80 %)** : « Qu'est-ce qu'une garantie d'origine et que prouve-t-elle vraiment ? », « Quels fournisseurs d'électricité verte comparer en Belgique ? », « Coopérative ou grand fournisseur : quelle vision du vert choisir ? », « Comment choisir… ». Réponse-first < 60 mots ajoutée en tête de chaque H2 reformulé. Corps, tableau comparatif et encadrés `<Tip>`/`<Warning>` intégralement préservés.

### meilleure-voiture.be — `blog/suv/meilleur-suv-belgique-2026` (audit priorité #3 + cannibalisation)
- **Constat câblage (garde-fous §5) :** deux fichiers portent le slug `meilleur-suv-belgique-2026` et rendent **deux URL distinctes** — `content/articles/…` → `/meilleur-suv-belgique-2026` (standalone via `lib/articles.ts`) et `content/blog/suv/…` → `/blog/suv/meilleur-suv-belgique-2026` (via `lib/blog.ts`). Le standalone est **déjà conforme** (83 % H2-questions, FAQ 7, aiSummary, liens sortants) → laissé tel quel (idempotent). L'article **blog/suv** était celui flaggé à **0 %**.
- **GEO :** H2 « Méthode / Top 5 / Tableau / Verdict » (0 % questions) reformulés en **5/6 questions (83 %)** avec réponse-first en tête, contenu, données TÜV/ADAC et tableaux préservés.
- **Anti-cannibalisation (par angle + lien pilier) :** l'article blog/suv est repositionné sur un **angle différenciant « fiabilité TÜV / consommation réelle ADAC »** (vs l'angle « usage & budget » du standalone), et **maille désormais vers le pilier** `/meilleur-suv-belgique-2026` + `/blog/suv/meilleur-suv-electrique-2026-belgique` + `/comparer` (était orphelin, 0 lien sortant). FAQ portée à 6.

### meilleure-voiture-7-places.be — liens cassés, JSON-LD, pilier orphelin (audit priorité #5)
- **`skoda-kodiaq-vs-peugeot-5008` — 5 liens en chemin racine nu → 404 corrigés.** Le repo sert les articles sous `/blog/[categorie]/[slug]` (confirmé sur un article correctement lié). Corrigés : `/meilleure-voiture-7-places-belgique-2026` ×3 → `/blog/suv-7-places/…` ; `/kia-ev9-vs-peugeot-e-5008` → `/blog/electriques/…` ; `/peugeot-5008-phev-vs-kia-sorento-phev` → `/blog/hybrides/…`.
- **`hyundai-santa-fe-vs-kia-sorento` — double JSON-LD nettoyé.** Retrait du bloc manuel `export const metadata = { structuredData: … }` (Article+FAQPage+Breadcrumb+Speakable) qui **doublonnait** le JSON-LD généré par le template depuis le frontmatter (confirmé transversalement par l'audit sur les 13 sites). Aucun autre article du repo ne porte ce bloc manuel → mise en cohérence.
- **Pilier `meilleure-voiture-7-places-belgique-2026` — maillage sortant ajouté.** Le pilier n'avait **aucun lien sortant**. Ajout de 4 liens internes contextuels (routes vérifiées) : `skoda-kodiaq-vs-peugeot-5008`, `hyundai-santa-fe-vs-kia-sorento`, `kia-ev9-vs-peugeot-e-5008` et l'asset `/comparer`.

### meilleure-fibre-internet.be — année en dur + maillage pauvre (audit priorité #1, volet SEO/GEO)
- **`proximus-fibre-avis-belgique` — année en dur retirée du title.** « … offres FTTH en 2026 ? » → « … offres FTTH en Belgique ? » (evergreen, sans réintroduire de token `[[date]]` non garanti sur ce site). Le H1 rendu est donc désormais sans année. Références datées du corps (« relevé juillet 2026 ») conservées comme signaux d'Expérience.
- **Maillage densifié (Proximus ↔ Digi).** Les 2 avis opérateurs ne pointaient que vers `/classement/…`. Ajout d'un lien réciproque contextuel : Proximus → [avis Digi], Digi → [avis Proximus], en plus du lien classement conservé.

---

## ⏭️ Reporté (au prochain run)

### meilleure-voiture-electrique — GEO à réaligner — **BLOCAGE DE BRANCHE (décision humaine requise)**
**Non traité ce run.** La branche par défaut du repo est `claude/no-image-spec-generator-nTjFC` ; **`main` est vide** (aucun `content/`). Le contenu (articles + `content/priorites-geo.md`) ne vit **que** sur la feature branch. Committer sur `main` comme demandé serait un **correctif fantôme** (garde-fous §5), committer sur la feature branch contredit le mandat « sur main ». **Même blocage que carte-credit `neobanques/` au run 2026-06-27.** → **Décision humaine :** confirmer la branche déployée sur Vercel comme cible, ou merger la feature branch dans `main` avant correction. Correctifs en attente : leads réponse-first, remonter `voiture-societe-electrique-2026` (67 %) et `byd-tang-vs-kia-ev9` (50 %) à ≥ 70 %, `aiSummary` dédié, étoffer `byd-tang-vs-kia-ev9` (~624 mots → ≥ 800).

### meilleure-fibre-internet.be — création des 2 articles (❌ CONTENU volume) — hors périmètre « fix-content »
Le site Configuré ne compte que **2 articles**. Produire du **contenu net-neuf** (nouveaux sujets) relève de la **rédaction / provisioning** (`emd-redaction` / seed), pas du domaine « correction de contenu existant » de ce run (garde-fous §4 : n'écrire que les fichiers pertinents au domaine). Les 2 articles existants sont désormais propres côté GEO/maillage. → À planifier via la tâche de rédaction.

### Contrepartie EN (FR + EN)
Les 7 correctifs de ce run portent sur le **FR** (les défauts flaggés étaient FR : `[[date]]`, H2, liens nus, JSON-LD, année en dur, maillage). Vérifier au prochain run que les miroirs EN correspondants (là où ils existent, ex. `content/blog/en`, `content/articles-en`) ne présentent pas les mêmes défauts (H2, liens nus).

### meilleure-voiture.be — dédoublonnage structurel `content/articles` ↔ `content/blog` (4 slugs piliers)
La cannibalisation a été **atténuée côté contenu** ce run pour le slug SUV (angle différencié + lien pilier), mais la **coexistence de deux URL** pour un même slug (standalone `/X` vs `/blog/cat/X`) sur les 4 piliers reste un **choix d'architecture/loader** (dédup `lib/blog.ts`/`lib/articles.ts`) → **run `emd-fix` mécanique**, comme acté au 2026-06-27.

### Années en dur dans slugs/filenames (réseau)
Plusieurs slugs conservent `-2026` (7-places pilier, familiale, neobanque, assurances). **Titres rendus** corrigés au fil de l'eau, mais le **renommage de fichier** casserait les liens entrants (garde-fous §2 : mettre à jour tous les consommateurs) → chantier de renommage + redirections à planifier à part.

---

## 🔎 À vérifier (câblage / rendu)
- **energie `electricite-verte` :** confirmer en prod que l'encadré IA et le FAQPage JSON-LD n'affichent plus « [[date]] » (frontmatter nettoyé).
- **7-places `skoda-kodiaq` :** confirmer que les 5 liens `/blog/[cat]/[slug]` résolvent en 200 (routes cibles vérifiées comme fichiers existants ce run).
- **7-places `santa-fe` :** confirmer qu'un **seul** jeu de JSON-LD (généré par le template) est émis après retrait du bloc manuel.
- **fibre `proximus` :** confirmer que le H1 rendu ne contient plus « 2026 ».

---

## Résumé
- **4 sites touchés**, **7 articles/piliers** corrigés sur `main`, **0 perte de données**, tout via `humaniser-fr` (FR).
- **Audit priorité #1 (fibre, volet GEO/SEO) :** année en dur + maillage traités ; création d'articles renvoyée à la rédaction.
- **Audit priorité #2 (energie) :** bug `[[date]]` frontmatter **résolu** + `electricite-verte` 40 % → **80 %** de H2-questions.
- **Audit priorité #3 (voiture SUV) :** article 0 % → **83 %**, étoffé, FAQ 6, liens sortants, cannibalisation atténuée (angle + lien pilier).
- **Audit priorité #5 (7-places) :** 5 liens 404 corrigés, double JSON-LD nettoyé, pilier orphelin maillé (4 liens).
- **Reporté :** voiture-electrique (**blocage de branche — décision humaine**), création d'articles fibre (rédaction), miroirs EN, dédoublonnage structurel voiture, renommage slugs `-2026`.
- **Références inchangées :** meilleur-suv.be, meilleure-voiture-utilitaire.be (déjà conformes).
