# Journal de correction EMD — 2026-06-18

Source : `pipeline/audits/LATEST.md` (audit 2026-06-18). Skill : `emd-fix` v1.3.0 + `humaniser-fr` + `seo-geo-redaction`.
Run autonome (tâche planifiée `emd-fix-weekly`). Tous les correctifs poussés directement sur la branche de production de chaque site (redéploiement Vercel automatique).

**Axe principal de ce run : le bloquant réseau n°1 — RGPD (7 sites sur 8) — + auteurs « la rédaction » (cluster Voiture) + assets template « 10min ».**

Composant déployé partout où le bandeau manquait : `CookieConsent` (Accepter/Refuser, choix mémorisé 365 j, FR + EN) + `GatedAnalytics` (Vercel Analytics chargé **uniquement après consentement**, via l'événement `cookie-consent-change`). DA unique préservée sur chaque site (couleurs du bandeau = palette du site). Idempotent.

Infos société injectées partout : **MentionBox SRL · SRL de droit belge · BE 0784.700.405 · Rue Blanche-Eau 15, 6950 Nassogne, Belgique.** Pages légales maintenues en `noindex`.

---

## Corrigé par site

### meilleure-voiture.be — ✅ RGPD + auteur
- **RGPD** : ajout `CookieConsent` + `GatedAnalytics` (Vercel Analytics gaté). Bandeau câblé dans `app/(site)/layout.tsx`.
- **Légal** : `mentions-legales` + `confidentialite` réécrites avec MentionBox SRL (placeholders `[À compléter]` supprimés), hébergeur Vercel corrigé (adresse + région fra1), politique cookies alignée sur la réalité (audience gatée au consentement), APD ajoutée. noindex conservé.
- **Auteur** : « La rédaction Meilleure Voiture » → **Julien Vanderhaeghe** (journaliste auto indépendant, Liège, depuis 2012), bio E-E-A-T `humaniser-fr`. Propagé partout (byline, page auteur, JSON-LD Person) via `niche.config` — les articles n'ont pas d'`author` en frontmatter, donc la bascule est globale et sans 404.
- Commits : `77d0eb2` (RGPD/légal), `ef91d5e` (auteur).

### meilleur-suv.be — ✅ RGPD + auteur + identité
- **RGPD** : `CookieConsent` (DA Carbone, cuivre/graphite) + `GatedAnalytics` + légal MentionBox SRL. Commit `b38dd79`.
- **Auteur** : « La rédaction Meilleur SUV » → **Damien Crols** (essais SUV, Namur, depuis 2014). Commit `3e2835e`.
- **Identité** : assets template « 10min » remplacés — `favicon.svg` (monogramme « S » cuivre), `logo.svg` (wordmark « meilleur·SUV » Space Grotesk), `og-default.svg` (DA Carbone). Plus de `#FF3D57`/Syne/« 10min ». Commit `1be1fee`.

### meilleure-voiture-familiale.be — ✅ RGPD + auteur + identité (site « Configuré »)
- **RGPD** : `CookieConsent` (DA Tribu, terracotta) + `GatedAnalytics` + légal MentionBox SRL. Commit `d6b541b`.
- **Auteur** : « La rédaction Meilleure Voiture Familiale » → **Audrey Pirard** (auto familiale, Wavre, depuis 2015). Commit `1c509b9`.
- **Identité** : assets template « 10min » remplacés — `favicon.svg` (« F » serif Fraunces terracotta), `logo.svg` (wordmark « Voiture Familiale »), `og-default.svg` (DA Tribu ivoire). Commit `ddd6fdc`.

### meilleure-voiture-electrique.be — ✅ RGPD (mensonge légal corrigé)
- **Branche de prod** : `claude/no-image-spec-generator-nTjFC` (≠ main pour ce repo).
- **RGPD** : **retrait complet de Google Analytics** (G-HN2MNQ8NKK) du layout — il était chargé alors que la politique de confidentialité affirmait « aucun Google Analytics ». Le retrait aligne le code sur la politique (et supprime un tracker chargé sans consentement effectif). Vercel Analytics cookieless conservé (déjà documenté, posture identique au site de référence carte-credit). Commit `ee84d2e`.

### quel-operateur-choisir.be — ✅ RGPD + page légale EN
- **RGPD** : `CookieConsent` FR (dans `(site)/layout`) **et** EN (dans `app/en/layout`) + `GatedAnalytics` + légal FR MentionBox SRL.
- **Page légale EN indexable** : `app/en/legal-notice` passée en **`noindex`** ; éditeur par défaut corrigé en « MentionBox SRL » + société complète + mention « analytics chargé uniquement après consentement ». Footer EN « MentionBox » → « MentionBox SRL ». Commit `3b61080`.

### meilleur-fournisseur-energie.be — ✅ RGPD (FR + EN)
- **RGPD** : `CookieConsent` FR + EN (bandeau ajouté dans `(site)/layout` et `app/en/layout`) + `GatedAnalytics` + légal FR MentionBox SRL (hébergeur + APD + cookies alignés). Commit `2faf0bf`.

### meilleures-assurances-auto.be — ✅ RGPD
- **RGPD** : `CookieConsent` (DA Sérénité, émeraude) + `GatedAnalytics` + légal MentionBox SRL (mention conseil FSMA, APD). Commit `18ac395`.

### meilleure-carte-credit.be — ⭐ référence, aucun correctif
- Déjà conforme (modèle répliqué sur tout le réseau). Non touché.

---

## Reporté (avec raison) — à traiter au prochain run

- **Purge du NL interdit** (`meilleur-suv.be` : `content/blog/nl/…` mort ; `meilleure-voiture-familiale.be` : `beste-gezinswagen-2026.mdx` rendu) — **bloqué par l'outillage** : les outils MCP disponibles (`github_write_file`, `github_commit_batch`) ne permettent pas la **suppression** de fichiers. À supprimer via un outil de delete ou un commit manuel.
- **GEO — reformulation des H2 en questions (≥ 70 %)** : `meilleure-voiture.be` (~0 %), `meilleure-voiture-familiale.be` (0 %), `meilleur-fournisseur-energie.be` (eau ~14 %, gaz PME ~12 %…), guides `meilleures-assurances-auto.be` (15-38 %), SUV familiaux/hybrides `meilleur-suv.be`. **Correctif lourd de structure** (édition multi-articles) — reporté pour rester sous le plafond et garantir zéro casse. Le contenu est déjà réponse-first : il s'agit surtout de reformuler les H2 déclaratifs.
- **Thin content < 800 mots** : `meilleure-voiture-familiale.be` (7 articles : Monospace ~620, SUV 7 places ~600, Break ~520, Hybride ~560, Pas chère ~510, EN Best family car ~430), `meilleur-suv.be` (SUV 7 places ~720, hybride ~740). **Réécriture lourde plafonnée** (SERP + ≥ 800 mots + FR/EN + `humaniser-fr`) — reportée (budget run + priorité donnée au RGPD réseau).
- **Avatar auteur (1:1) + page auteur enrichie + bio EN** pour Julien/Damien/Audrey : le type `niche.author` n'a pas de champ avatar ; câbler un avatar généré nécessite de toucher `AuthorCard`/page auteur. La bascule de nom/bio/JSON-LD est faite ; avatar + longBio multi-paragraphes + bio EN à compléter.
- **Speakable JSON-LD** absent (voiture, suv, familiale, energie, assurances), **sitemaps statiques** (cluster Voiture) et **hreflang sitemap cassé** (carte-credit) : correctifs techniques non traités ce run.
- **Multilingue réel FR↔EN** sur le cluster Voiture (switcher non monté, zéro article EN) et **parité EN** energie (3 guides EN seulement) : refonte i18n — hors périmètre d'un run de correction sûr.
- **Type de home incohérent / refontes DA** : non traité (signalé dans l'audit, refonte risquée).

---

## Bilan run
- **Sites touchés : 7** (carte-credit non touché, déjà conforme).
- **RGPD : 7/7 sites non conformes corrigés** (bandeau + gating Analytics + société + noindex).
- **Auteurs persona uniques : 3/3** du cluster Voiture (fin du « la rédaction » anonyme).
- **Identité (assets « 10min ») : 3/3** sites flaggés corrigés.
- **Opérations lourdes (thin content) : 0** réalisées / **~9 articles** reportés (sous le plafond de 15).
- **Casse : aucune** (édits ciblés, idempotents). Pages légales restées en noindex.
