# Build — PAGES CLÉS remplies — 2026-07-04

**Tâche :** `emd-build-pages` (samedi 17h) · **Mode :** ÉCRITURE sur `main`, data-driven uniquement, sans affiliation.
**Doctrine :** `references/pages-cles.md`, `skills/humaniser-fr`, `skills/seo-geo-redaction`.
**To-do source :** `pipeline/audits/pages-LATEST.md` (audit 2026-07-04).
**Périmètre traité ce run :** les 2 sites **data-driven** neufs signalés « safe / data-fill JSON » par l'audit : `meilleure-fibre-internet.be` et `meilleur-fournisseur-electricite.be`. Les 11 sites « TS hardcodé » restent hors périmètre de fill (voir « Reportées »).

---

## meilleure-fibre-internet.be — Télécom / Fibre

Site conforme méthodo (`content/data/*.json` + quiz YAML). Tout le remplissage = édition de données → **safe, build non touché.**

| Page clé | Avant | Après | Fichier |
|---|---|---|---|
| Comparateur | placeholder démo (« Modèle A/B/C ») | **2 slugs réels** : `fibre` (8 offres : Digi, Telenet, Edpnet, Scarlet, Mobile Vikings, Orange, VOO, Proximus) + `internet-tv` (5 packs) | `content/data/comparateurs.json` + `.en` |
| /choisir | `{}` | **rempli** : `fibre` + `internet-tv` (tldr, sections, tableaux, FAQ) | `content/data/choisir.json` |
| Quiz | questions démo | **questions fibre réelles** (débit, zone, engagement) ; `categorie` route vers slugs réels | `content/pages/quiz.yaml` + `quiz.en.yaml` (créé) |
| Classements | 1 seed (`meilleure-fibre-internet`, 6 items) | **+1 cluster** : `meilleur-operateur-internet` (7 opérateurs, ≥1000 mots, FR+EN, sourcé juillet 2026) | `content/data/classements.json` + `.en` |
| Conformité | `deals.yaml` contenait `affiliate_disclaimer` | **retiré** (règle « aucune affiliation ») | `content/pages/deals.yaml` |

**Données :** prix d'entrée relevés juillet 2026 (Digi 10 €, Telenet 36 €, Orange 45 €, VOO 49 €, Proximus Giga Fiber 2 Gbps 57,99 €, Mobile Vikings 40 €). Sources : BIPT, Test-Achats, Astel, grilles opérateurs. Liens `url`/`sourceUrl` = pages officielles neutres, aucun affilié.

## meilleur-fournisseur-electricite.be — Énergie / Fournisseur

Même architecture data-driven. Remplissage = données → **safe.**

| Page clé | Avant | Après | Fichier |
|---|---|---|---|
| Comparateur | placeholder démo | **2 slugs réels** : `electricite` (8 fournisseurs : Mega, Bolt, Ecopower, TotalEnergies, Luminus, Eneco, OCTA+, Engie) + `electricite-verte` (6, notés Greenpeace) | `content/data/comparateurs.json` + `.en` |
| /choisir | `{}` | **rempli** : `electricite` + `electricite-verte` (fixe/variable, coopératives, FAQ) | `content/data/choisir.json` |
| Quiz | questions démo | **questions énergie réelles** (conso, tarif, région) ; `categorie` route vers slugs réels | `content/pages/quiz.yaml` + `quiz.en.yaml` (créé) |
| Classements | 1 seed (`meilleurs-fournisseurs-electricite`, 7 items) | **+1 cluster** : `meilleure-electricite-verte` (6 items, ≥1000 mots, FR+EN) | `content/data/classements.json` + `.en` |
| Conformité | `deals.yaml` contenait `affiliate_disclaimer` | **retiré** | `content/pages/deals.yaml` |

**Données :** prix indicatifs 3 500 kWh/an (juin 2026, CREG/VREG/CWaPE) + classement environnemental **Greenpeace 2026** (Ecopower & Cociter 20/20, EnergyVision 18/20, Aspiravi 17/20, Trévion 16/20, Bolt 13/20). Le classement vert corrige la perception « Bolt = plus vert » : les coopératives sont devant.

---

## À CODER (composant NON data-driven — reporté, non touché ce run)

Signalé par l'audit et confirmé à la lecture du code ; **jamais bricolé** (rule 3, ne pas casser le build) :

1. **`/deals` — array inline `const DEALS: Deal[] = []` dans `app/(site)/deals/page.tsx`** (fibre ET electricite). Le `deals.yaml` n'est PAS consommé par la page (FAQ/marquee en dur). Remplir un tableau de prix = éditer du TSX → **à coder.** Seul le `affiliate_disclaimer` du YAML a été retiré (donnée, safe).
2. **Reco quiz — `QuizEngine.recommend()` est un placeholder hardcodé** (« Produit recommandé / Modèle placeholder / À définir »). Les **questions** sont data-driven (YAML, remplies) et les CTA routent désormais vers des slugs réels, mais le **texte de recommandation** reste figé → **à coder.**
3. **/choisir EN** : `lib/choisir-content.ts` n'importe que `choisir.json` (pas de `.en`). L'éditorial /choisir est donc FR only côté template → **à câbler (build-risqué), reporté.**
4. Les **11 sites « TS hardcodé »** (voiture, suv, familiale, utilitaire, 7-places, opérateur, énergie, assurances-auto, carte-crédit…) : tout remplissage = édition de TS → hors boucle de fill. Voir `pages-2026-06-27.md` et la synthèse de l'audit.

## Vérifications

- Parité i18n : chaque clé FR de `comparateurs.json`/`classements.json` a son miroir EN (mêmes `specsLabels`, même nombre de `modeles`/`items`) → conforme à `tests/i18n-parity.test.ts`.
- Slugs comparateur = clés `choisir.json` = valeurs `categorie` du quiz → pages `/choisir/[slug]` et `/comparer/[slug]` générées et éditorialisées.
- Accord en genre : `fibre`/`électricité verte` féminin (« meilleure »), `opérateur`/`fournisseur` masculin (« meilleur(s) ») — respecté dans titres/labels.
- Aucun lien affilié, aucun CTA d'achat, aucun disclaimer affilié. Liens sortants = sources officielles neutres.

**Bilan :** 2 sites, 4 comparateurs (slugs) remplis, 4 pages /choisir, 4 quiz (FR+EN), 2 classements ajoutés (FR+EN), 2 disclaimers affiliés retirés. Cap classements (2-3/run/site) respecté : 1 nouveau cluster/site, le reste au run suivant.
