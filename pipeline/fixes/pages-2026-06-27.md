# Journal — BUILD PAGES CLÉS

**Date :** 2026-06-27
**Tâche :** `emd-build-pages` (samedi 17h)
**Référence :** `references/pages-cles.md`, `pipeline/audits/pages-LATEST.md`
**Mode :** écriture sur `main`, **data-driven uniquement**, **sans affiliation**.

> Principe appliqué : ne remplir que les composants **data-driven** (JSON consommé par le template). Tout composant à logique figée signalé par l'audit (TS hardcodé, arrays inline `page.tsx`, `recommend()` placeholder) est **logué « à coder (build-risqué) »** et **non touché**, pour ne jamais casser le build.

---

## Sites traités ce run (2 sites, data-driven)

### 1. meilleure-neobanque.be — Banque (conforme méthodo, `content/data/` + `lib/classement.ts` câblés FR/EN)

**Comparateur — `content/data/comparateurs.json` (+ `.en.json`)**
- Constat audit : 2 slugs × **4 items (< 5)** → sous-seuil.
- Action : +1 item factuel par slug, traités équitablement, `sourceUrl` neutre (site officiel), aucun lien affilié.
  - `neobanques` → ajout **Aion Bank** (IBAN belge BE, licence BNB) → **5 items**.
  - `comptes-pro` → ajout **N26 Business** (offre Standard gratuite, licence BaFin) → **5 items**.
- FR + EN. Commit : `df5988b`.

**Classements (GEO asset #1) — `content/data/classements.json` (+ `.en.json`)**
- Constat audit : 1 seed (`meilleures-neobanques`). Clusters restants sans classement.
- Action : **2 clusters complétés** (cap respecté : 2/run), chacun **≥ 1000 mots**, dans le système existant du template (`lib/classement.ts` + `/classement/[produit]`). Schéma réel respecté + champ `genre`.
  - **`meilleures-neobanques-voyage`** (cluster *frais-etranger*) — `genre: f`, titre « Meilleures néobanques pour voyager et payer à l'étranger ». 5 items réels notés/100 (Wise, Revolut, N26, bunq, Aion), 5 sections H2 en questions (answer-first), 7 FAQ, criteria + methodology + sources datées (NBB, FSMA, Wise), prix datés « relevé juin 2026 ».
  - **`meilleures-neobanques-pro`** (cluster *comptes-pro*) — `genre: f`, titre « Meilleures néobanques pour indépendants et freelances ». 5 items (Qonto, Revolut Business, N26 Business, bunq Business, Wise Business), 6 sections H2 en questions, 7 FAQ, sources datées (SPF Finances, FSMA, NBB).
- FR + **miroir EN strict** (mêmes slugs → parité `tests/i18n-parity.test.ts`). Commits : FR `a180990`, EN `02a26b8`.
- Vérif : relecture du JSON FR committé → structure valide (3 clusters, fermeture correcte, 5 items/cluster).

### 2. meilleure-voiture-7-places.be — Voiture (conforme méthodo `content/data/`)

**/choisir éditorial — `content/data/choisir.json`**
- Constat audit : `choisir.json = {}` (**EMPTY**), `.en.json` absent.
- Action : rempli **4 produits** alignés sur les slugs comparateur (maillage `/comparer/[slug]`), voix auteur Sophie Lambrechts, factuel, accord féminin (« voiture 7 places »).
  - `voiture-7-places`, `suv-7-places`, `voiture-7-places-electrique`, `voiture-7-places-pas-cher`.
  - Chacun : `tldr` (4) + 2 `sections` (paragraphs + tip/internalLink) + `faq` (3). Données prix/conso/autonomie factuelles (catalogue 2026 BE), marques multiples traitées équitablement, aucun lien affilié.
- FR uniquement (voir « à coder » ci-dessous). Commit : `54f5859`.

---

## Reporté « À CODER (build-risqué) » — non touché ce run

Conformément à la doctrine (ne pas écrire de logique de composant). À planifier hors boucle de fill :

**meilleure-neobanque.be**
- `/deals` et `/simulateur` : arrays inline dans `page.tsx` (non data-driven).
- Moteur de reco quiz (`QuizEngine.recommend()`) : enrichissements futurs.

**meilleure-voiture-7-places.be**
- **EN /choisir** : `lib/choisir-content.ts` n'importe que `choisir.json` (pas de `.en.json`). Le rendu EN de /choisir n'est **pas câblé** → ajouter un `choisir.en.json` ne serait pas consommé. **À coder** (locale-awareness de `getChoisirContent`), comme déjà fait pour `lib/classement.ts`.
- `/deals` + reco quiz placeholder (déjà signalés par l'audit).

**Autres sites de l'audit (non traités ce run, non data-driven ou hors cap)**
- Classements **ABSENTS** (route+lib+data from scratch = à coder) : `meilleure-voiture-utilitaire.be`, `quel-operateur-choisir.be`, `meilleur-fournisseur-energie.be`, `meilleures-assurances-auto.be`, `meilleure-carte-credit.be`.
- Classements en **TS hardcodé** (à refactorer vers `classements.json`) : `meilleure-voiture.be`, `meilleur-suv.be`, `meilleure-voiture-familiale.be`, `meilleure-voiture-electrique.be`.
- Sites **« tout TS »** (comparateur/choisir dans `lib/*.ts`, pas de `content/data/`) : voiture, familiale, opérateur, énergie, assurances-auto, carte-crédit → tout remplissage = édition TS = à coder.
- `/deals` & `/simulateur` inline : **9 sites** (cf. audit).

---

## Candidats data-driven pour le prochain run
- **meilleure-voiture-7-places.be** : compléter `choisir.json` (slugs `monospace-7-places`, `voiture-7-places-hybride`) ; ajouter clusters classements manquants (`classements.json` déjà câblé, 6 slugs présents → vérifier couverture).
- **meilleure-neobanque.be** : 3e cluster classement (`meilleures-neobanques-epargne` — ex. Trade Republic 4 % cash, bunq, Revolut, N26, Aion) ; enrichir `/choisir` (`comptes-pro` = 1 section).
- **meilleur-suv.be / meilleure-voiture-utilitaire.be** : `/choisir` JSON (data-driven) à compléter/allonger.

---

## Commits
- `df5988b` — neobanque : comparateur ≥5 items/slug (FR+EN)
- `a180990` — neobanque : classements FR (+2 clusters GEO)
- `02a26b8` — neobanque : classements EN (miroir)
- `54f5859` — 7-places : choisir FR (×4)
