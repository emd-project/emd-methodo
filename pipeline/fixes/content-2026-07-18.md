# Journal des correctifs — domaine CONTENU & SEO/GEO — 2026-07-18

**Périmètre (hérité de `content-LATEST.md`, ≤14j non audités) :** 1 site — `quel-fournisseur-energie.be`.
**Doctrine chargée :** `emd-fix`, `seo-geo-redaction`, `humaniser-fr`.
**Verdict d'audit :** ⚠️ (aucun ❌ bloquant). Thin content : 0. Articles < 70 % H2-questions : 0 (moy. 87 %).
**Contrainte de run :** précis, idempotent, zéro casse ; FR + EN ; commits clairs sur `main`.

---

## ✅ Corrigé

### `quel-fournisseur-energie.be` — commit `e6a0ab2` (`main`)

1. **Année dynamique dans les titres** (audit action #1, volet titres — GEO/structure).
   - `lib/blog.ts` : ajout de `withDynamicYearTitle(title, publishedAt)`, appliqué dans `parseMeta` au seul champ `title`. Remplace l'occurrence de l'**année de publication** par `currentYear()`.
   - Chokepoint unique : `parseMeta` alimente H1, `<title>`/OG (generateMetadata), JSON-LD Breadcrumb + Article (`headline`), et toutes les cartes/listings — FR **et** EN (les deux passent par `parseMeta`).
   - **Idempotent & sans churn** : no-op tant que `year(publishedAt) === currentYear()` (donc aucun rendu modifié en 2026 : les titres restent corrects), bascule automatique au 1er janvier suivant via l'ISR quotidien (`revalidate = 86400`).
   - **Sûr** : cible uniquement l'année de parution (jamais un millésime historique cité dans un titre) et le seul champ `title`. Les chiffres datés du corps et des descriptions (tarif CREG T3 2026, accises avril 2026, etc. — légitimement datés par doctrine) restent inchangés. Zéro perte de contenu, aucun fichier d'article réécrit.
   - Couvre les 4 titres FR + 4 titres EN (tous « en 2026 » / « in 2026 »).

2. **`Speakable` ajouté au JSON-LD** (audit action #3 — GEO/données structurées).
   - `components/article/ArticleView.tsx` : ajout de `SpeakableSpecification` (`cssSelector: ['h1', '.standfirst', '#en-bref']`) au nœud `Article`. Additif, sans effet de bord, FR + EN.

**Vérifications effectuées avant/après commit :** validation de syntaxe TS/TSX (esbuild) des deux fichiers ; test unitaire de la regex d'année (`2026`→`2027` au changement d'année, no-op à millésime égal) ; diffs surgicaux confirmés (seules les lignes visées changent) ; relecture du fichier committé (`lib/blog.ts`) — typographie FR et échappement `\b` intacts.

---

## ⏭️ Reporté (au prochain run, avec raison)

1. **Slugs datés** (`electricite/prix-kwh-electricite-belgique-2026`, `gaz/prix-gaz-belgique-2026`) — audit actions #1 (volet slugs) et #4 (redirections).
   - **Raison :** un renommage propre exige (a) supprimer les anciens fichiers FR + EN, (b) mettre à jour le mapping `lib/i18n/article-slugs.ts`, (c) corriger les liens internes qui pointent vers ces slugs (le corps de `gaz` FR **et** EN lie l'article `electricite` par son slug daté), (d) poser des **redirections 301** old→new (FR + EN). L'outillage GitHub disponible ici est *create/update-only* (pas de suppression) : renommer sans supprimer laisserait l'ancienne URL en ligne = **contenu dupliqué**, pire que le slug daté. À exécuter en opération dédiée et vérifiée (zéro URL morte). Articles publiés cette semaine → équité SEO minime, report peu coûteux.

2. **Rééquilibrage des sujets persona × marques** (audit action #2) et **tags marques+persona** (action #5).
   - **Raison :** ce sont de **nouveaux articles** (comparatifs « Quel fournisseur pour [famille/locataire/gros conso/PME] », « Mega vs Bolt », « 100 % verts »), pas des correctifs d'articles existants — relève de la tâche de **rédaction quotidienne** (`seo-geo-redaction`), hors du mandat « fix » de ce run. Recommandation transmise ci-dessous.

3. **Année en dur dans les descriptions (frontmatter)** — extension possible de l'action #1.
   - **Raison :** hors périmètre explicite de l'audit (qui vise titres + slugs) ; impact GEO mineur (meta description). Éviter de réécrire 6 fichiers d'articles lourds pour un gain marginal ; le mécanisme d'année dynamique pourra y être étendu proprement si souhaité.

---

## Reste à surveiller (non bloquant)

- Ratio ⅔ marques-modèles (× persona) / ⅓ info à mesure que le blog grandit (actuellement 4/4 informationnels).
- Anti-cannibalisation (frontière head-nu respectée aujourd'hui).
- Ne pas régresser sur la structure GEO (% H2-questions, réponse-first, FAQ 7, aiSummary 5).

_Aucun audit écrasé. Ce journal ne modifie pas `content-LATEST.md`._
