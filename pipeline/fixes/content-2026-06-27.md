# Journal des correctifs — Domaine CONTENU & SEO/GEO — 2026-06-27

**Tâche :** `emd-fix-content` (run planifié) · **Mode :** ÉCRITURE sur `main` des repos de site.
**Doctrine appliquée :** `skills/emd-fix` v1.6.0 + `skills/seo-geo-redaction` v1.5.1 + `skills/humaniser-fr` v1.1.2 + `references/garde-fous.md`.
**Source de la to-do :** `pipeline/audits/content-LATEST.md` (audit 2026-06-27).
**Périmètre traité :** GEO/structure (H2 en questions, réponse-first, FAQ 6-7, aiSummary/TL;DR, année dynamique), maillage interne (liens valides, anti-cannibalisation), thin content (< 800 mots → ≥ 800).
**Plafond :** ~15 opérations lourdes/run. **Réalisé :** 8 opérations lourdes, toutes sur `main`, tout le texte rédigé via `humaniser-fr` (FR ; jamais NL).

---

## ✅ Corrigé

### meilleure-voiture-familiale.be — 6 piliers à 0 octet remplis (audit priorité #2)
Les 6 fichiers `.mdx` piliers étaient vides (blob git `e69de29`), ce qui cassait le maillage interne (les 8 articles cluster pointaient vers des pages vides → liens cassés + clusters orphelins). Chacun a été rempli **en place** (overwrite d'un fichier vide = zéro perte de données) avec un comparatif GEO complet : lead réponse-first 40-60 mots, **H2 100 % en questions**, pattern Answer-Explanation-Example, tableau comparatif chiffré, **FAQ 7**, **aiSummary 5 bullets**, `<StatCard>`/`<Verdict>`, 2-4 liens internes contextuels (cluster + `/comparer`), signé `audrey-pirard`. Marques/modèles réels cités (Skoda, Volkswagen, Dacia, Toyota, Hyundai, Kia, Peugeot, Citroën, Renault…), accord au genre respecté.

- `content/blog/breaks/meilleur-break-familial-2026.mdx` — rempli (≈ 1 050 mots).
- `content/blog/monospaces/meilleur-monospace-familial-2026.mdx` — rempli (≈ 1 050 mots).
- `content/blog/monospaces/monospace-7-places-pas-cher.mdx` — rempli (≈ 1 050 mots).
- `content/blog/hybrides/meilleure-voiture-familiale-hybride-2026.mdx` — rempli (≈ 1 100 mots).
- `content/blog/suv-familiaux/meilleur-suv-familial-7-places-belgique.mdx` — rempli (≈ 1 100 mots).
- `content/blog/budget/voiture-familiale-pas-chere-2026.mdx` — rempli (≈ 1 050 mots).

**Effet de bord positif :** les liens internes du cluster (ex. `break-grand-coffre-familial` → `meilleur-break-familial-2026`) sont **réparés automatiquement** par le remplissage, sans toucher aux articles liants. Titres rédigés **sans année en dur** (slug `-2026` conservé pour ne pas casser les liens entrants — voir « Reporté »).

### meilleur-fournisseur-energie-be — `changer-fournisseur-energie-belgique` (audit priorité #1, triple défaut)
Article corrigé sur trois axes en un seul passage :
- **GEO :** H2 reformulés en questions strictes → **100 % de H2 en question** (était ~20-40 %, flag à 20 %). Réponse-first < 60 mots en tête de chaque H2.
- **Thin :** corps enrichi de ~700 à **≈ 1 000 mots**, sourcé (loi du 25/08/2012, CWaPE/BRUGEL/VREG, gestionnaires Sibelga/ORES/RESA/Fluvius, protection hivernale), sans perte du contenu existant (étapes, tableau préavis conservés).
- **FAQ :** portée de **4 à 7** questions (ajout : compteur digital, économies chiffrées, facture de clôture).
- **Année dynamique :** « (2026) » retiré du `title` ; `[[date]]` déjà géré par le template ailleurs sur le site.
- Maillage : liens vérifiés vers `/comparer/electricite`, `/comparer/gaz`, `/choisir/electricite`, `fournisseur-electricite-belgique-guide`, `resilier-contrat-energie-belgique`, `contrat-fixe-variable-energie-belgique`.

### meilleures-assurances-auto.be — `guides/bonus-malus-assurance-auto-belgique` (audit priorité #1)
- **GEO :** 8 H2 reformulés → **7/8 en question stricte (87,5 %)** (était 25 %), réponse-first en tête de chaque section. **Contenu intégralement préservé** (tables, `<StatCard>`, `<Tip>`, exemples chiffrés) : seuls les intitulés et phrases d'ouverture ont changé.
- **FAQ :** portée de **6 à 7** (ajout : « Faut-il toujours déclarer un petit sinistre ? »).
- **Année en dur :** « 2026 » retiré du `seoTitle` (le site n'a pas de mécanisme `[[date]]` ; suppression = evergreen, sans risque).
- Voix `tu` (auteur `thomas-renard`) conservée ; schéma FAQ `question/answer` conservé (shim `lib/blog.ts` OK).

---

## ⏭️ Reporté (au prochain run)

### meilleure-carte-credit.be — cluster `neobanques/` (12 comparatifs « X vs Y » à ~20 % de H2-questions) — **audit priorité #1**
**Non traité ce run — blocage de branche, pas de capacité.** Deux raisons :
1. **Conflit branche / source rendue (bloquant).** La branche par défaut du repo est `claude/setup-nextjs-apple-guide-En4gb` ; **`main` ne contient PAS le contenu** (`content/blog/neobanques` vide/inexistant sur `main`). Committer la correction sur `main` (comme demandé par la tâche) serait un **correctif fantôme** (garde-fous §5) : la prod rend la feature branch. Décision conforme à l'INVARIANT « zéro casse » : **ne rien committer à l'aveugle**. → **Décision humaine requise** : confirmer la branche déployée (feature branch) comme cible, ou merger la feature branch dans `main` avant correction.
2. **Volume.** 12 articles (tout le sous-dossier) dépassent la capacité restante d'un run de qualité.
Patron à appliquer (identique aux autres) : reformuler les H2 descriptifs (« L'IBAN belge : avantage Revolut ») en questions (« Revolut ou N26 : lequel offre un IBAN belge ? »), réponse-first, FAQ 6-7. Même remarque pour le **dédoublonnage** `carte-credit-independant-belgique.mdx` (présent dans `guides/` ET `pro-business/`).

### Contrepartie EN (FR + EN)
Les 8 correctifs de ce run sont **FR uniquement**. Les défauts flaggés (liens cassés familiale, thin/GEO énergie, GEO assurance) étaient côté FR. Les **miroirs EN** des 6 piliers familiale + des 2 articles énergie/assurance sont à produire au prochain run (reconstruction i18n hors périmètre « contenu » strict de ce run).

### Années en dur dans les slugs/filenames (familiale)
3 piliers conservent `-2026` dans le **nom de fichier/slug** (`meilleur-break-familial-2026`, `meilleur-monospace-familial-2026`, `meilleure-voiture-familiale-hybride-2026`, `voiture-familiale-pas-chere-2026`). **Volontairement non renommés** ce run : le renommage casserait tous les liens entrants du cluster (garde-fous §2 : énumérer et mettre à jour TOUS les consommateurs dans le même commit). Les **titres rendus** sont déjà sans année en dur. Chantier de renommage + mise à jour des liens à planifier à part.

### Autres items hors périmètre « contenu » (renvoyés au domaine concerné)
- Cannibalisation `meilleure-voiture.be` (4 slugs piliers `content/articles` ↔ `content/blog`), stubs liés `meilleur-suv.be` (404 internes), gabarit `article-modele.mdx` publié sur 7-places, Speakable/shim FAQ `lib/articles.ts` : relèvent du dédoublonnage structurel / template (mécanique) — **à traiter par le run `emd-fix` mécanique**, pas par ce run contenu.

---

## 🔎 À vérifier (câblage)
- **Images de couverture des 6 piliers familiale :** `featureImage` pointé vers les covers d'articles frères existants (mêmes dossiers `/blog/<cat>/<slug-frère>/…-cover.jpeg`) pour garantir un rendu non cassé. À confirmer visuellement sur Vercel ; idéalement générer des covers 16:9 dédiées au prochain passage images.
- **Rendu des 6 piliers :** fichiers désormais non vides avec frontmatter valide (pas de `draft`) → doivent être rendus par le loader `lib/blog.ts`. À confirmer en prod.

---

## Résumé
- **3 sites touchés**, **8 articles** corrigés/remplis sur `main`, **0 perte de données**, tout via `humaniser-fr`.
- **Audit priorité #2 (piliers vides familiale) : traité à 100 %** (6/6) + maillage interne réparé.
- **Audit priorité #1 (GEO comparatifs/guides) : traité partiellement** — énergie « changer » (✅) et assurance « bonus-malus » (✅) ; **carte-credit `neobanques/` reporté pour blocage de branche** (décision humaine requise).
- **Reporté :** carte-credit (12, blocage branche), miroirs EN, renommage des slugs `-2026`, items structurels/mécaniques.
