# Journal des corrections — TECHNIQUE & i18n — 2026-06-21

**Source to-do :** `pipeline/audits/tech-LATEST.md` (2026-06-20)
**Doctrine :** `skills/emd-fix/SKILL.md` + `references/i18n-multilingue.md`
**Mandat :** SEO technique + multilingue réel, sur `main`, **zéro casse**, câblage vérifié.
**Contrainte clé :** pas de build de vérification disponible → tout chantier i18n build-risquant est **reporté** (« build check requis ») plutôt que poussé à l'aveugle (cf. §3 de la tâche + règle BUILD de la doctrine i18n).

---

## ✅ Corrigé (commits sur `main`)

### meilleure-voiture.be — SEO tech (sitemap/hreflang 404) + garde locale
Commit `f4ecae4` — 4 fichiers, additif et idempotent, FR intact.
- **Cause racine :** le site déclare `locales: ['fr','en']` mais n'a **aucun arbre `app/en/`** → toutes les cibles `/en/*` sont des **404**. Le sitemap (`mirror()` + boucles `niche.locales`) et le helper `alternatesFor()` (via `mirrorPaths` + pages article) publiaient donc des `<url>`/`hreflang` `/en` pointant vers du 404.
- **Fix (sans risque build) :**
  - Nouveau `lib/i18n/served-locales.ts` → `servedLocales()` = `[defaultLocale]` tant que `app/en/` n'existe pas. Documenté et **réversible** (retourner `niche.locales` une fois l'arbre porté).
  - `app/sitemap.ts` : n'itère plus que les **locales servies** → plus aucune URL/hreflang `/en` 404.
  - `lib/i18n/alternates.ts` : `alternatesFor()` **filtre** les locales non servies → corrige le hreflang de **toutes** les pages (statiques + articles) en un point central, `x-default` conservé sur la locale par défaut.
  - `lib/blog.ts` : ajout `RESERVED_LOCALE_DIRS` ({en,nl,de,es,it}) → `content/blog/en` ne peut plus être lu comme une **fausse catégorie** FR (anti-pattern doctrine). Additif, comportement FR inchangé (aujourd'hui aucune fausse catégorie ne sortait).
- **Câblage vérifié :** `app/sitemap.ts`, `lib/i18n/alternates.ts` (consommé par les pages via `alternatesFor`), `lib/blog.ts` sont bien dans le chemin de rendu. `robots.ts` déjà OK, `app/opengraph-image.tsx` présent et unique (inchangé).

### meilleur-fournisseur-energie-be — garde locale
Commit `eb2fae2` — `lib/blog.ts`.
- Ajout `RESERVED_LOCALE_DIRS` ({en,nl,de,es,it}) sur la version nue du template (action #3 de l'audit). `content/blog` est vide → **préventif**, mais bannit définitivement le risque de fausse catégorie `en`/`nl`. Additif, build-safe.

### meilleure-voiture-electrique — OG : câblage CONFIRMÉ (pas de correctif nécessaire)
- L'audit notait l'OG « à confirmer » (pas de `app/opengraph-image.tsx` à la racine, pattern `[locale]`). **Vérifié :** `app/[locale]/opengraph-image.tsx` **existe et est câblé** → OG/Twitter image correctement générée par locale. **Rien à corriger.** (Lecture faite sur la branche de déploiement, cf. note ci-dessous.)

---

## ⏳ Reporté — « i18n à porter (build check requis) »

### meilleure-voiture.be — arbre `app/en/` complet
Porter `app/en/` (layout, pages, blog, légal) sur un `main` auto-déployé **sans build check casserait potentiellement tout le déploiement, FR inclus** (règle BUILD de `references/i18n-multilingue.md`). À faire en **incréments compilables** avec préview Vercel, en copiant le pattern de `quel-operateur-choisir.be`. **Non poussé à l'aveugle.**
- Conséquence assumée tant que reporté : le `LangSwitch` propose toujours EN (UX) et les articles EN orphelins de `content/blog/en/{hybrides,suv}` restent non routés. Le **SEO** est néanmoins protégé (plus de 404 en sitemap/hreflang).
- Reliquat mineur noté : `generateStaticParams` de `app/(site)/blog/[categorie]/[slug]/page.tsx` itère encore `niche.locales` → peut pré-générer des params de slugs EN (soft-404 au runtime sous locale FR, **hors sitemap donc non indexés**). À nettoyer lors du port i18n (gater par `servedLocales()`), fichier volumineux non réécrit à la main pour éviter tout risque de casse.

### meilleur-fournisseur-energie-be — EN réel ou retrait du scaffold
`app/en/` est un **scaffold creux** (pas de lecteurs `…En()` dans `lib/blog.ts`, pas de source de contenu EN ; `niche.locales` = `['fr']` alors que `app/sitemap.ts` code en dur une section `/en`). Rendre l'EN réel **ou** retirer l'arbre `app/en/` + ses imports (`comparateur.en`, `en-guides`, `PRODUIT_SLUGS_EN`) sont **deux chantiers build-risquants** → **reportés** (build check requis). Non touché pour ne pas risquer la casse du déploiement.

---

## 🚫 Bloqué par l'outillage (à refaire avec un outil de delete Git)

### Suppression NL — meilleur-suv.be & meilleure-voiture-familiale.be
- `content/blog/nl/` présent sur les deux (suv : compacts/familiaux/hybrides ; familiale : `beste-gezinswagen-2026.mdx`). Doctrine « jamais de NL » → à supprimer.
- **Impossible dans ce contexte :** le connecteur GitHub disponible n'expose que read / write / commit (`github_write_file`, `github_commit_batch`) — **aucun outil de suppression de fichier**. Écrire un fichier vide ≠ supprimer (et violerait l'INVARIANT anti-écrasement).
- **Risque actuel : nul.** Les deux sites ont déjà la garde locale (`LOCALE_BLOG_DIRS`/`RESERVED_LOCALE_DIRS`) → le NL **n'est pas lu comme catégorie**, ni routé. Nettoyage cosmétique à effectuer via `git rm` lors d'un run disposant d'un outil de delete.

---

## ➖ Hors périmètre TECHNIQUE de ce run

- **meilleure-voiture-electrique :** branche `main` **vide** — le site vit sur sa branche de déploiement `claude/no-image-spec-generator-nTjFC`. Mandat = `main` → **aucune écriture** faite hors `main`. OG vérifié en lecture seule (OK, ci-dessus).
- **meilleure-voiture-utilitaire.be :** `content/blog` vide (0 article FR/EN). Le **provisioning de contenu** relève du domaine contenu/GEO, pas de ce run technique. Structure i18n déjà exemplaire (rien à corriger côté tech).
- **Conformes / idempotents (aucune action) :** quel-operateur-choisir.be (réf.), meilleure-carte-credit.be (réf.), meilleures-assurances-auto.be, et la structure i18n de meilleur-suv.be / meilleure-voiture-familiale.be.

---

## Récap

| Site | Action | Statut |
|---|---|:--:|
| meilleure-voiture.be | sitemap/hreflang `/en` 404 supprimés + garde locale | ✅ `f4ecae4` |
| meilleur-fournisseur-energie-be | garde locale (préventif) | ✅ `eb2fae2` |
| meilleure-voiture-electrique | OG câblé confirmé | ✅ (vérif.) |
| meilleure-voiture.be | arbre `app/en/` | ⏳ build check requis |
| meilleur-fournisseur-energie-be | EN réel / retrait scaffold | ⏳ build check requis |
| meilleur-suv.be | suppression `content/blog/nl` | 🚫 pas d'outil delete |
| meilleure-voiture-familiale.be | suppression `content/blog/nl` | 🚫 pas d'outil delete |

**Aucune perte de données. Tous les changements additifs/idempotents, FR intact, câblage vérifié.**
