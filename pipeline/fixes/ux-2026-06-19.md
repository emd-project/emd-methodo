# Journal des correctifs — DA · UX · Images · Responsive · A11y

**Date :** 2026-06-19 · **Doctrine :** `skills/emd-fix/SKILL.md` v1.5.1 (+ `references/garde-fous.md`)
**Audit source :** `pipeline/audits/ux-LATEST.md` (2026-06-19) · **Mode :** écriture sur `main`, ciblée, idempotente
**Périmètre de ce run :** Images · Responsive · DA/identité (mécanique) · A11y. Hors périmètre : favicon (1:1), OG, contenu/rédaction, refonte de direction DA.

> Principe §0 appliqué : on corrige la **source réellement rendue** et on **vérifie le câblage**. Un fichier au bon nom mais non câblé n'est pas corrigé (anti correctif fantôme).

---

## ✅ Corrigé (commits sur `main`)

### 1. meilleure-voiture.be — logo éclair réseau → mark compas naval unique
- **Fichier rendu :** `components/layout/Nav.tsx` (SVG inline, PAS `public/logo.svg`).
- **Avant :** `path d="M13 2 4.5 13.5H11l-1 8.5L19.5 10H13l0-8Z"` = éclair générique partagé (footprint #1).
- **Après :** mark **compas 4 branches concave** `d="M12 1.5 14.4 9.6 22.5 12 14.4 14.4 12 22.5 9.6 14.4 1.5 12 9.6 9.6Z"` (navigation / « Auto Naval »).
- **Câblage vérifié :** `volteo.css` → `.nav .logo .mark svg path { fill: var(--primary); }` → mark rendu en **naval #1C3FAA** sur pastille `--ink`. Mécanique CSS inchangée (path sans `fill` inline). ✔️
- **Commit :** `97d7c22`.

### 2. meilleur-fournisseur-energie-be — logo éclair réseau → mark flamme énergie unique
- **Fichier rendu :** `components/layout/Nav.tsx` (SVG inline).
- **Avant :** même éclair générique `M13 2 4.5 13.5…` (footprint #1), avec `fill="#FF6B35"` inline **mort** (écrasé par la règle CSS ci-dessous).
- **Après :** mark **flamme** (single path) `d="M12 2C12 2 6 8 6 14a6 6 0 0 0 12 0c0-2.5-1.2-4.3-2.4-6C14.8 9.6 13.6 10 13 9c-.8-1.4.2-4.2-1-7Z"`. `fill` inline retiré.
- **Câblage vérifié :** `globals.css` → `.nav .logo .mark svg path { fill: var(--primary); }` → rendu en `--primary` (#3D5AFE), **inchangé en couleur** (pur changement de forme, zéro régression palette). ✔️
- **Commit :** `9017b52`.

### 3. meilleure-voiture.be — tokens DA génériques résiduels → palette naval
- **Fichier rendu :** `app/styles/volteo-overrides.css` (chargé **en dernier**, `html:root` spécificité 0,1,1 = source effective des accents).
- **Avant :** `--aurora-3` = vert générique (`#006B4F`/`#3DFFC0`) et `--shadow-accent` = `rgba(200,0,31,…)` (rouge template) — seuls tokens encore génériques **à l'écran** (cf. faux positif ci-dessous). `--aurora-3` est rendu dans le dégradé de `.comparateur-card-wrap`.
- **Après :** `--aurora-3: var(--cat-2)` (teal naval) ; `--shadow-accent: 0 0 24px color-mix(in srgb, var(--primary) 22%, transparent)` (naval). Token-driven, aucun hex brut dans `volteo.css :root`.
- **Commit :** `69eea99`.

---

## 🔎 À vérifier / faux positif d'audit (câblage)

- **meilleure-voiture.be — « split-brain accents globals.css » = FAUX POSITIF partiel.** L'audit signalait que les `--accent-*` de `globals.css` (défaut `#FF3D57`/`#C8001F`) rendaient les corps d'articles en rouge générique. **Or `app/styles/volteo-overrides.css` (importé en dernier, `html:root`) remappe déjà** `--accent-1..5`, `--aurora-1/2`, `--text-*`, `--bg-*` sur la palette naval (`var(--primary)`, `var(--spark-text)`, `var(--cat-2/3)`). Éditer `globals.css :root` aurait été un **correctif fantôme** (sans effet à l'écran, la couche overrides gagne par ordre de cascade à spécificité égale). Seuls `--aurora-3` + `--shadow-accent` étaient effectivement génériques → corrigés (cf. #3).
  - **Reco méthodo :** `emd-audit` doit lire la **couche overrides chargée en dernier** avant de conclure à un split-brain, sinon ré-émission du même faux positif.

---

## ⏸️ Reporté / signalé (non auto — garde-fous : build-risqué, non-Live, ou hors périmètre)

### Directions DA génériques — refonte NON auto (signalée)
- **meilleur-fournisseur-energie-be — direction DA non assumée (❌ audit).** Skin V1 « Électrique » par défaut **non muté** : `--primary #3D5AFE`, vars `--elec/--gaz/--green/--water/--mobi` brutes, **pas de `volteo.css` de surcharge ni de `overrides.css` accents**. C'est le seul site sans direction assumée.
  - **Recommandation (à faire par un humain, pas auto sur site live) :** muter la palette hors du défaut `#3D5AFE` via une surcharge `:root` dédiée (sur le modèle de `meilleure-voiture.be`) + fonts distinctives. Direction suggérée pour la niche énergie : **« Flux »** — vert-émeraude profond + cuivre chaud (chaleur/économie), OU bleu-nuit + ambre. Diversifier (anti-footprint) sans homogénéiser avec les autres sites.
  - **Déjà fait (mécanique) :** mark flamme unique posé dans `Nav.tsx` (cf. #2).

### A11y — `<html lang>` figé sur sites bilingues (build-risqué : logique de layout)
- **Sites concernés :** `meilleur-suv.be`, `meilleure-voiture-familiale.be`, `quel-operateur-choisir.be`, `meilleure-carte-credit.be` — `lang="fr"` en dur dans le root layout.
- **Pourquoi non auto :** ces sites V1 ont un root layout **synchrone** sans helper de résolution de locale. `meilleure-voiture.be` (et `meilleure-voiture-electrique`) servent déjà `lang` **dynamique** via `@/lib/i18n/server-locale` (`getLocale()`, layout `async`, `lang={locale}`). Porter ce pattern = ajout d'infra + passage du layout en async = **modification de logique de layout** → `references/garde-fous.md` §2 = **signaler**, pas bricoler en autonome (pas de filet de build).
  - **Recommandation :** porter le helper `getLocale()` + `lang={locale}`. **À vérifier d'abord :** présence d'un `app/en/layout.tsx` (en App Router, seul le root layout porte `<html>` ; `/en/` hérite donc du `lang` racine).
  - `meilleures-assurances-auto.be` : `lang="fr"` figé **acceptable** (mono-locale `['fr']`).

### Accents split-brain — meilleure-voiture-familiale.be (non-Live)
- **Constat câblage :** familiale **n'a pas** de `volteo-overrides.css`, et son `volteo.css` (Tribu) ne remappe pas `--accent-*`/`--aurora-*`. Donc ici, contrairement à `meilleure-voiture.be`, les accents génériques de `globals.css` **sont** la source effective (audit correct).
- **Pourquoi non auto :** site **« Configuré » (non Live)** + blog 100 % vide (`.mdx` à 0 octet) → aucun article ne rend ces accents aujourd'hui. Le correctif câblage-correct exige soit une réécriture de `globals.css` (24 Ko, sans filet de build) soit l'ajout d'un import dans `layout.tsx` (build-touch). Faible urgence → **signalé**.
  - **Recommandation :** créer `app/styles/volteo-overrides.css` calqué sur `meilleure-voiture.be` (`html:root` remap `--accent-*`/`--aurora-*`/`--shadow-accent` sur la palette « Tribu » via `var(--primary)`/`--cat-*`) + import en dernier dans `layout.tsx`.

### Config désynchronisée — meilleur-suv.be (build/config)
- `niche.config.ts` annonce `mode:'light'` + palette défaut, mais le rendu réel est « Carbone » **dark** (`data-theme="dark"` en dur dans `layout.tsx`). Resynchroniser `niche.config` (source de vérité) = touche config → **signalé**, non auto.

### Images
- **meilleur-fournisseur-energie-be :** les cartes « related » rendent `<ImagePlaceholder>` rayé au lieu de la `featureImage` réelle de l'article → **logique de composant** (build-risqué) → signalé. Les 20 articles ont bien leurs `featureImage` `.webp` en `next/image`.
- **Blogs vides** (suv : 1 draft ; familiale : `.mdx` 0 octet ; carte-credit : `_example` draft, `featureImage:""`) → **rien à wirer** (pas de contenu) → hors périmètre images de ce run.
- **meilleure-voiture-electrique :** pas de cover d'article = **choix éditorial assumé** (commenté dans le code), pas un défaut.
- **Aucune cover de catégorie manquante, aucun doublon de featured, aucun chemin cassé** détecté sur les sites Live à contenu (audit ✅). → **Aucune image générée** (pas de manque réel ; pas de génération spéculative).

### Hors périmètre de ce run (relevé pour run dédié « identité »)
- **Favicon (1:1) :** `meilleur-fournisseur-energie-be/app/icon.svg` contient **encore l'éclair générique** (`#3D5AFE`) → footprint #1 résiduel **sur le favicon**. `meilleure-voiture.be` : pas d'`app/icon.svg` → favicon non servi. → à aligner sur les nouveaux marks dans un run favicon.
- **OG (16:9) :** `meilleure-carte-credit.be/app/opengraph-image.tsx` (néon V1 par défaut) et `meilleure-voiture-electrique` (OG + icon palette rouille obsolète) désalignés sur la DA réelle.
- **Branches par défaut ≠ `main` :** `meilleure-carte-credit.be` (`claude/setup-nextjs-apple-guide-En4gb`) et `meilleure-voiture-electrique` (`claude/no-image-spec-generator-nTjFC`). Tout correctif doit cibler la branche **réellement déployée** — à confirmer avant écriture. **Aucune écriture faite** sur ces deux repos ce run.

---

## ✅ Sans action requise
- **Responsive — 0 scroll horizontal** confirmé par l'audit sur les 8 sites (`overflow-x: clip/auto`, `max-width:100%` médias, `viewport`, tables wrappées). Rien à corriger.
- **A11y (autres)** : `prefers-reduced-motion`, `:focus-visible`, skip-link présents sur les sites V1 inspectés.

---

## Récap
- **3 commits** sur 2 repos (`meilleure-voiture.be` ×2, `meilleur-fournisseur-energie-be` ×1).
- **Footprint « éclair réseau partagé » (#1)** éradiqué dans les **logos Nav** des 2 sites concernés (résiduel sur le favicon energie = hors périmètre, signalé).
- **1 faux positif d'audit** identifié et documenté (split-brain voiture déjà câblé via overrides).
- **Directions DA signalées (refonte non auto) :** meilleur-fournisseur-energie-be.
- **Câblage restant à confirmer / reporté :** lang dynamique ×4, accents familiale, config suv, related-cards energie, favicon/OG, branches non-main.
