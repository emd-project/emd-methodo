# Journal des corrections — domaine `ux` (DA · UX · IMAGES · RESPONSIVE · A11y)

**Date :** 2026-07-19 · **Source to-do :** `pipeline/audits/ux-LATEST.md` · **Écrit sur `main`.**
**Périmètre (hérité de l'audit) :** quel-fournisseur-energie.be, meilleur-abonnement-5g.be. Aucun autre site touché.

Câblage vérifié à la source rendue (§0 doctrine). INVARIANT anti-perte-de-données respecté :
aucun article vidé, aucune écriture de vide, édits ciblés, aucun renommage de symbole
(valeurs CSS aliasées → aucune référence pendante).

---

## quel-fournisseur-energie.be

### ✅ Corrigé
- **Covers de catégorie RENDUES (audit action #1).** `CategoryView` (hub-hero) ne rendait aucune
  image. Ajout d'un rendu `next/image` 16:9 dans le hub-hero + chapô de catégorie (description
  `niche.config`). Câblage data-driven via **`lib/category-covers.ts`** (nouveau) qui mappe
  chaque slug → `public/images/categories/<slug>.jpeg` (les 5 covers existaient, désormais
  affichées). Responsive sans media-query (flex-wrap + aspect-ratio).
  - Commits : `feat(category): map de covers…`, `feat(category): rendre la cover + le chapô…`
- **DA transverse — hex en dur `volteo.css :root` purgés (audit action #4).** `--elec/--gaz/--green/
  --water/--mobi` (+ `-soft`) étaient des hex en dur hors-palette → aliasés sur `var(--accent-1..5)`
  (mêmes noms conservés, seules les valeurs suivent la DA). `.news { background: var(--green) }`
  (vert `#1AA35F` hors-palette) → `var(--accent-3)`. Texte des `.tag.elec/.green/.water/.mobi`
  dérivé en `color-mix` (plus de couleur figée). Chaque site rend désormais SA palette même si
  `volteo.css` reste partagé → l'empreinte réseau disparaît au rendu.
  - Commit : `fix(da): volteo.css — teintes catégorie + .news aliasées sur la palette`
- **Identité — favicon aligné (audit action #5).** `app/icon.svg` était un monogramme « Q » serif,
  différent du mark rendu dans `Nav.tsx` (anneau de bascule + flamme). Remplacé par le **même tracé
  SVG** (mark inline du Nav) sur fond ink `#1B1813`, tinté brique `#8E2F3C`. Favicon 1:1.
  - Commit : `fix(identite): favicon aligné sur le mark Nav`

### ⏭️ Reporté au prochain run
- **`featureImage` de `content/blog/fournisseurs/fournisseur-energie-verte-belgique.mdx` (❌ audit
  action #2)** + **stop du recyclage des 2 images in-body** (qui pointent sur des covers de
  catégorie). Ces 3 corrections exigent des images 16:9 UNIQUES. **Le service de génération
  d'images (`generate_image`/Nano Banana) était indisponible ce run** : deux lots de 3 images
  (prompts éditoriaux puis simplifiés) sont restés `pending` > 15 min chacun puis ont expiré sans
  produire de fichier (`list_recent_images` ne montre aucun `energie-verte-be-*`). Choix
  doctrine : **ne PAS recycler une cover de catégorie en featured** (ce serait ajouter l'empreinte
  que l'audit dénonce) ni écraser du contenu — `featureImage` laissé en l'état, à régénérer et
  wirer au prochain run quand le service répond.

### ℹ️ Mineur, non repris (risque/valeur)
- `globals.css @theme` omet `--color-accent-5` (`#6B4E71`, catégorie « changer »). Impact limité :
  les slots `--cat-5`/`.tag.c5` consomment le token `:root` `--accent-5` (présent). Seuls d'éventuels
  utilitaires Tailwind `*-accent-5` seraient concernés. Non repris pour éviter une réécriture
  complète d'un fichier de 20 Ko pour un token quasi inutilisé — à ajouter au prochain passage DA.

---

## meilleur-abonnement-5g.be

### ✅ Corrigé
- **DA — `globals.css @theme` synchronisé (audit action #3).** Le bloc `@theme` gardait la palette
  TEMPLATE (`--color-accent-1: #FF3D57` rouge, ambre, etc.) alors que `da-site.css` n'override que
  les tokens `:root` `--accent-*`. Les utilitaires Tailwind `*-accent-*` (générés au build depuis
  `@theme`) rendaient donc du rouge/ambre hors-DA. `@theme` réécrit sur la palette turquoise du
  site (`#4FD1C5 / #C9F24D / #FF8A5B / #9B8CFF / #5AA9FF` + fonds/texte/sémantique dark + ajout
  `--color-accent-5`). Blocs light/`data-theme` template laissés (neutralisés par `da-site.css`
  `!important`, dark fixe).
  - Commit : `fix(da): synchroniser @theme sur la palette turquoise du site`
- **Covers de catégorie RENDUES (audit action #1).** Même correctif `CategoryView` que l'énergie
  (branche `presse` préservée). `lib/category-covers.ts` (nouveau) mappe les slugs →
  `a5g-cat-operateurs/forfaits/couverture-v2/prix/mvno.jpeg` (existants).
- **DA transverse — `volteo.css` (audit action #4).** Même purge des hex en dur / `.news` que
  l'énergie (fichier byte-identique, corrigé à l'identique → rendu divergent car tokens propres).

### ⏭️ / ℹ️
- Images in-body recyclées (⚠️ audit) : non traité ce run (service de génération indisponible,
  cf. énergie) — à traiter au prochain run avec des visuels uniques.
- Logo « proche d'un glyphe wifi générique » (⚠️ audit) : jugé thématiquement juste (5G) et
  distinct du mark énergie ; pas de refonte (idempotence — pas d'over-engineering d'un mark
  acceptable).

---

## À vérifier (câblage prouvé mais rendu non observé en runtime)
- Rendu visuel des covers dans le hub-hero sur les 2 sites (câblage `CategoryView` → `lib/category-
  covers` → fichiers existants = prouvé statiquement ; capture d'écran non réalisée).
- Neutralité de `.news` (aucune section newsletter montée sur les 2 `homeSections`) → changement
  cosmétique inerte, sans risque.

## Assets dont le câblage reste 100 % confirmé
- `app/icon.svg` (énergie) : Next émet `<link rel="icon">` sur `app/icon.svg` (convention) → servi.
- Covers de catégorie : fichiers présents dans `public/images/categories/` sur les 2 sites.
