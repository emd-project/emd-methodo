# Journal des corrections — DA · UX · Images · Responsive · A11y

**Date :** 2026-06-21 · **Tâche :** `emd-fix-ux` (mandat élargi) · **Doctrine :** `skills/emd-fix/SKILL.md` v1.6.0
**Audit source :** `pipeline/audits/ux-LATEST.md` (2026-06-19, révisé propriétaire)
**Principe :** correction de la SOURCE rendue + vérification du câblage ; zéro perte de données ; commits sur la branche déployée de chaque site.

> Constat d'entrée : une grande partie des bloquants de l'audit avaient déjà été corrigés par les runs des 19–20/06 (responsive nav des 4 sites, logo voiture.be, OG carte-credit). Ce run traite **ce qui restait réellement** + entreprend le gros chantier DA de l'énergie.

---

## Gros chantiers entrepris

### ❌→✅ meilleur-fournisseur-energie-be — DA refaite (« Cuivre chaud »)
Seul site **sans direction DA assumée** : `globals.css :root` portait encore le skin V1 « Électrique » par défaut (`--primary #3D5AFE` bleu électrique générique, `--spark` lime `#CCFF48`), partagé avec le template/réseau.

- **Palette mutée** dans `app/overrides.css` (chargé **après** `globals.css` → l'emporte ; câblage vérifié dans `app/layout.tsx`) : primaire cuivre-bronze **`#B45309`**, `--primary-d #8A3D06`, `--primary-soft #FBEBD3`, spark or `#F5B301`. Direction **unique dans le réseau** (anti-footprint) et **énergie-thématique** : le primaire « signe l'urgence d'action » (cf. `niche.config.signature.oneRule`). Les **couleurs sémantiques par énergie** (élec/gaz/vert/eau/bornes) restent inchangées (lisibilité métier).
- **Mark (flamme)** du `Nav.tsx` : suit automatiquement `--primary` via `.nav .logo .mark svg path { fill: var(--primary) }` → repeint en bronze sans toucher le composant. (L'éclair générique flaggé par l'audit avait déjà été remplacé par une flamme lors d'un run précédent ; la **paire footprint n°1** éclair voiture.be↔énergie est donc éteinte.)
- **Favicon** `app/icon.svg` : flamme repeinte `#3D5AFE → #B45309`.
- **OpenGraph** : `app/opengraph-image.tsx` lit `niche.config.palette.accent1` → `niche.config.ts` mis à jour `#3D5AFE → #B45309`. OG désormais cohérent (barre d'accent bronze→orange→vert).
- **Contraste AA vérifié** : blanc/`#B45309` = 5.1:1 (boutons, badges) ; `#8A3D06`/blanc ≈ 8:1 (liens prose, eyebrow, lettrine).
- Commits : `69d7b33` (overrides.css + icon.svg), `fbf1528` (niche.config.ts). Idempotent : DA posée une fois, à ne pas churner.

---

## Correctifs ciblés

### ❌→✅ meilleure-voiture-electrique — OG + favicon réalignés sur la DA bleue
L'OpenGraph (crème `#F8F5EF` + rouille `#C4390E`) et `app/icon.svg` (bronze `#C17E2A`) portaient l'**ancienne DA chaude** ; le site live est bleu froid. Réalignés sur les tokens réels lus dans `globals.css` (`--accent-1 #0F62E8`, `--bg-primary #F1F4F8`, `--text-primary #0D1B2A`).
- Bonus : **retrait de la mention « NL »** dans le sous-titre OG (« FR/NL/EN » → « FR/EN » ; le réseau est FR/EN, jamais NL).
- Commit `3fd9d1d` sur la branche déployée `claude/no-image-spec-generator-nTjFC` (la branche `main` du repo est vide ; Vercel suit la branche par défaut).

### ⚠️→✅ meilleure-voiture-familiale.be — accents alignés sur la DA « Tribu » (action audit #5)
`globals.css` portait des accents **génériques** (rouge `#C8001F` partagé réseau) pour les liens prose, la lettrine, l'**aurora hero** et le fond des tableaux. Remappés dans `volteo-overrides.css :root` sur les tokens « Tribu » de `volteo.css` : `--accent-1: var(--primary-d)` (sapin-teal foncé, liens/lettrine), `--accent-4: var(--primary)` (fond thead, texte blanc), `--accent-3: var(--cat-2)` (pétrole), `--aurora-*` sur primary/spark/pétrole. AA vérifié (liens ≥6:1 sur ivoire ; thead+blanc ≥5:1). Mode clair (light-only) ; idempotent. Commit `8c04828`.

### ⚠️→✅ A11y — `<html lang>` aligné sur la locale d'URL (sites bilingues à lang figé)
Layouts racines **statiques** (lang="fr" figé) sur sites bilingues fr/en. Plutôt que de rendre le layout dynamique (risque pour la génération statique / CWV), ajout d'un **script inline `<head>`** qui aligne `document.documentElement.lang` sur le préfixe d'URL (`/en` → `en`, sinon `fr`). Le ciblage SEO reste porté par `hreflang` (alternates.languages).
- `quel-operateur-choisir.be` (`8da6c15`), `meilleure-carte-credit.be` (`78c4e58`, branche `claude/setup-nextjs-apple-guide-En4gb`), `meilleure-voiture-familiale.be` (script `<head>` thème étendu).
- **Incident corrigé en cours de run** : un premier commit familiale (`1566a60`) avait introduit `t('common.skipToContent', undefined, locale)` (ReferenceError : `locale` non défini dans ce scope) → corrigé immédiatement par `e3673ac` (retour à `t('common.skipToContent')`). Build de nouveau compilable.

---

## Déjà conforme (idempotent — aucune action)

- **Responsive nav (4 sites)** — le scroll horizontal mobile était déjà neutralisé : `volteo-overrides.css` de voiture.be / suv / familiale (masquage `.nav-cta` ≤940px + `overflow-x: clip`), et `app/responsive-fix.css` (importé) de l'électrique (breakpoint burger remonté à ≤960px, `.nav-burger` forcé visible — concorde avec `MobileNav`, pas de bande « sans nav »).
- **meilleure-voiture.be** — mark voiture sur mesure (plus l'éclair), accents `globals.css` déjà remappés naval dans `volteo-overrides.css` (`html:root`), `app/icon.svg` naval présent. Split-brain résolu.
- **meilleure-carte-credit.be (OG)** — `opengraph-image.tsx` déjà en DA « papier » (crème + rouge + vert forêt + or + motif carte), plus le néon V1 flaggé par l'audit.
- **Images énergie** — les 6 covers de catégorie (`public/images/categories/*.webp`) existent et sont câblées via le fallback `ArticleCard.coverFor` (featureImage → cover catégorie) ; pas de `<ImagePlaceholder>` rayé dans le chemin de rendu actuel.
- **meilleur-suv.be (lang /en)** — corrigé par design via le script de `app/en/layout.tsx` (cf. note du layout racine) ; non retouché.

---

## À vérifier (câblage non prouvé à 100 %)

- **énergie — pont Tailwind `@theme --color-accent-1`** : `overrides.css :root` redéfinit aussi `--color-accent-1: #B45309`. La bascule des **utilitaires Tailwind v4** (`bg-accent-1`/`text-accent-1`, employés par quelques composants `comparer/`, `quiz/`, `choisir/`) dépend de l'ordre des couches CSS. Le chemin principal (`--primary` / `--accent-1`) est, lui, vérifié. À confirmer visuellement sur ces composants après redéploiement.
- **Branches déployées électrique & carte-credit** : commits poussés sur les branches **par défaut** des repos (`claude/...`), `main` étant absente/vide. À confirmer que Vercel production suit bien ces branches.

---

## Reporté (hors domaine DA/UX/Images/Responsive/A11y)

- **énergie — incohérence i18n** : `Nav.tsx` rend un `LangSwitch → EN` inconditionnel et un dossier `app/en` existe, alors que `niche.config.locales = ['fr']` (mono-locale). `<html lang="fr">` est cohérent avec le mono-locale ; la cohérence sélecteur/contenu EN relève du **domaine i18n**.
- **suv — `niche.config.ts` désynchronisé** du rendu (annonce `mode:'light'`/palette générique alors que le rendu réel est « Carbone » dark). Le **rendu** est correct et muté ; resynchroniser la config est un correctif de cohérence config (risque sur OG/composants lisant `niche.style.mode`) → à traiter prudemment hors run UX.
- **Blogs vides** (suv : 1 draft ; familiale : `.mdx` à 0 octet ; carte-credit : `_example` draft) → **domaine contenu/rédaction**.

---

## Sites touchés ce run
`meilleur-fournisseur-energie-be` (DA refaite), `meilleure-voiture-electrique` (OG+favicon), `meilleure-voiture-familiale.be` (accents Tribu + a11y lang), `quel-operateur-choisir.be` (a11y lang), `meilleure-carte-credit.be` (a11y lang).

*Aucune perte de données. Éditions ciblées. INVARIANT anti-perte respecté (pas de read-modify-write post-écriture). Un incident de build a été détecté et corrigé dans le run même.*
