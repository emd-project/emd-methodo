# Journal de correction — TECHNIQUE & i18n — 2026-07-04

**Base :** `pipeline/audits/tech-LATEST.md` (audit du 2026-06-20).
**Mandat de ce run :** SEO technique + multilingue RÉEL uniquement, sur `main`, idempotent, zéro casse, câblage vérifié. i18n = build-risquant → aucun `app/en` poussé à l'aveugle sans build check.
**Mode d'exécution :** lecture du rendu réel du code (routes, middleware, lecteurs, sitemap). Pas de fetch HTTP de prod.

> Constat global : les **2 bloquants ❌ de l'audit** (voiture.be « pseudo-multilingue » et energie « multilingue creux ») ont **déjà été résolus par un run/provisioning antérieur**. Ce run les a **re-vérifiés au niveau du câblage** (idempotent → aucune réécriture). Les items ⚠️ restants ne sont pas actionnables sans risque ou sans outil manquant → reportés avec raison.

---

## ✅ Vérifié conforme (déjà corrigé — aucune modification, idempotent)

### meilleure-voiture.be — était ❌ PSEUDO-MULTILINGUE → désormais câblé
Résolu via une architecture i18n **middleware-rewrite** (`localePrefix: 'as-needed'`), pas un arbre `app/en/` littéral. Câblage vérifié fichier par fichier :
- `middleware.ts` : réécrit `/en/<path>` → `/<path>` en interne et transmet la locale via header `x-mv-locale`. `matcher` exclut `sitemap.xml`/`robots.txt`/assets. → `/en/*` renvoie 200, pas 404.
- `lib/blog.ts` : garde `RESERVED_LOCALE_DIRS` présente (locales non-défaut + nl/de/es/it) → aucun dossier de locale lu comme fausse catégorie.
- `content/blog/en/{suv,citadines,electriques,hybrides,occasions}` présents (miroir EN).
- `lib/i18n/article-slugs.ts` : mapping FR↔EN peuplé (14 paires).
- `app/sitemap.ts` : n'émet une URL EN d'article/catégorie **que si** le mapping + le fichier EN existent (`servedLocales()` + garde à la source) → plus de cible hreflang en 404. `app/opengraph-image.tsx` présent à la racine.
→ **Bloquant de l'audit levé. Rien à changer.**

### meilleur-fournisseur-energie-be — était ❌ MULTILINGUE CREUX → désormais câblé
- `lib/blog.ts` : garde `RESERVED_LOCALE_DIRS` (`en,nl,de,es,it`) désormais présente (l'audit la disait absente).
- Moteur EN réel : `lib/en-guides.ts` (registre `EN_GUIDES`, 3 guides, `getEnCategories()`), `lib/comparateur.en.ts` (`PRODUIT_SLUGS_EN`). Arbre `app/en/` complet (blog, compare, choose, guides, simulator, quiz, deals, legal-notice).
- `app/sitemap.ts` : les cibles `/en/*` pointent vers des routes existantes (200) ; hreflang réciproque posé **uniquement** sur les paires statiques stables (`L()`), pas sur les slugs non 1:1 → pas de faux hreflang.
→ **Bloquant fonctionnel levé au plan routing/technique.** Réserve : parité de contenu EN encore **mince** (3 guides, pas de MDX EN) = amélioration de fond du **domaine contenu**, hors périmètre de ce run technique.

### Gardes de locale (`lib/blog.ts`) — action #3 de l'audit
Vérifiées présentes sur **voiture.be** et **energie** (les deux sites que l'audit signalait « nus »). suv / familiale / utilitaire déjà OK selon l'audit. → **Risque de fausse catégorie `en`/`nl` fermé sur tout le réseau audité.**

---

## ⏸ Reporté (avec raison) — pas de casse, pas de correctif fantôme

### Suppression du NL — suv & familiale (action #4 de l'audit)
- `meilleur-suv.be` : `content/blog/nl/{compacts,familiaux,hybrides}/*.mdx` présents.
- `meilleure-voiture-familiale.be` : `content/blog/nl/beste-gezinswagen-2026.mdx` présent.
- **Bloqueur outil :** le connecteur GitHub de ce run n'expose que read / write / list / commit_batch — **aucune primitive de suppression**. Écraser par du vide violerait l'INVARIANT anti-perte-de-données (et ne supprimerait pas le fichier). Risque nul aujourd'hui : ces dossiers sont **exclus des catégories** par la garde de locale (pas de fausse catégorie rendue).
- **À faire au prochain run disposant d'un outil de suppression git** (ou `git rm` manuel) : retirer ces arbres NL.

### OG à confirmer — meilleure-voiture-electrique (action #5 de l'audit)
- La branche **`main` de ce repo est vide** ; le code réel vit sur la branche par défaut `claude/no-image-spec-generator-nTjFC`. Le mandat de ce run est strictement `main`.
- → Confirmation de `app/opengraph-image` (pattern `[locale]`) **non réalisable sur `main`**. À traiter sur la branche active, hors périmètre `main` de ce run. Aucune modification tentée (éviter tout correctif hors scope).

### Provisionnement contenu — meilleure-voiture-utilitaire.be (action #5)
- `content/blog` = `.gitkeep` (0 article FR/EN). Structure i18n exemplaire mais coquille.
- → **Domaine contenu + gros chantier build-risquant** (FR+EN à provisionner) : hors périmètre technique, et non poussable à l'aveugle sans build check. Reporté au pipeline de provisioning.

---

## 🧱 i18n à porter (build check requis) — non tenté ce run
Aucun arbre `app/en` complet poussé à l'aveugle (règle de sécurité build : une erreur de compil casse tout le déploiement Vercel, FR inclus). Les sites audités ont déjà leur socle i18n en place (middleware ou `app/en`) → **aucun portage à l'aveugle nécessaire ni tenté**. La cause racine (porter le système i18n dans `emd-template`) reste un chantier séparé à faire sous build check.

---

## Synthèse
- **Corrigé ce run :** 0 (tout le safe-fixable de l'audit était déjà en place → idempotent).
- **Re-vérifié conforme / câblage prouvé :** voiture.be (middleware i18n), energie (moteur EN + garde), gardes de locale réseau.
- **Reporté :** NL suv+familiale (pas d'outil de suppression git), OG electrique (`main` vide), contenu utilitaire (hors périmètre).
- **Build-check requis :** aucun portage `app/en` à l'aveugle — non nécessaire (socles i18n déjà présents).
- **Casse :** aucune. **Perte de données :** aucune.
